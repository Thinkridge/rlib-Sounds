# Examples

## smftowav

Convert Standard MIDI File (.mid) to WAV using SoundFont (.sf2).

## Run example

This example is written in TypeScript and executed with `tsx`.
No JavaScript build output is generated.

```bash
npm install
npm run example
```
